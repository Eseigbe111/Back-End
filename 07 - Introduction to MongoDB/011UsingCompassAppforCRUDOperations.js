// Run this command "mongosh --file .\copy_db4.js" to copy all of natours-test3 to natours-test4. U do that b4 running mongosh.exe
// So for this lecture, we will work with this file "natours-test4"

// Now instead of using the terminal to work with MongoDB, we can also use an app with a graphicaluser interface that MongoDB provides
// us which is called Compass. So lets now go ahead and download the compass application.

// Watch this video if u forget anything. It is about doing all we did in the terminal with the MongoDB CompassApp(which is GUI i.e Graphic User Interface).
//Andd this is how we will do it in our Express app later.
